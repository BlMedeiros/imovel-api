interface Property {
    id: ID!
    price: Float
    totalArea: Float
    localization: Localization!
    propertyStatus: PropertyStatus
}

enum PropertyStatus {
    DRAFT,
    AVAILABLE,
    SOLD
}
